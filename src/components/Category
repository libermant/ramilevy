import Product from './Product';

const Categories = ({ category }) => {
  return (
    <div>
      <h1>{category.categoryName}</h1>
      {category.productsList.map((prod, index) =>
        <Product key={index} product={prod} />
      )}
    </div>
  );
};

export default Categories;
