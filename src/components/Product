const Product = ({ product }) => {
    return (
        <div>
            <h1>{product.name}</h1>
            <h3>{product.description}</h3>
            <h4>{product.price}</h4>
            <img src={product.image} />
        </div>
    );
}

export default Product
